import { TouchableOpacity } from 'react-native';
import styled from 'styled-components/native';
import { StyledProps } from '@/types';

export const DefaultBackButton = styled(TouchableOpacity)<StyledProps>`
  margin-right: 16px;
`;