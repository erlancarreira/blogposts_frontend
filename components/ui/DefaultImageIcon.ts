import { StyledProps } from '@/types';
import styled from 'styled-components/native';

const DefaultImageIcon = styled.Image<StyledProps>`
  width: 24px; 
  height: 24px; 
`;

export default DefaultImageIcon;